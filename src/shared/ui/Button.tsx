/**
 * Button — re-export alias for IOSButton
 * Preserves the import path for legacy consumers.
 */
export { IOSButton as Button } from './buttons/IOSButton';
export type { IOSButtonProps as ButtonProps } from './buttons/IOSButton';
